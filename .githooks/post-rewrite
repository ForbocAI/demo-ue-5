#!/usr/bin/env bash
# Re-locks the SDK submodule after rebases and amend rewrites.

set -euo pipefail

PROJECT_ROOT="$(git rev-parse --show-toplevel)"
bash "$PROJECT_ROOT/Scripts/lock_sdk_submodule.sh" --lock
